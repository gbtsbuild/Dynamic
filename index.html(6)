<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modern E-Commerce</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Arial', sans-serif;
        }

        :root {
            --primary-color: #ff6b6b;
            --secondary-color: #4ecdc4;
            --dark-color: #2d3436;
            --light-color: #f9f9f9;
        }

        /* Header Styles */
        header {
            background: var(--dark-color);
            padding: 1rem;
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 100;
        }

        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            max-width: 1200px;
            margin: 0 auto;
            color: white;
        }

        .cart-counter {
            background: var(--primary-color);
            padding: 0.5rem 1rem;
            border-radius: 20px;
            cursor: pointer;
        }

        /* Main Content */
        main {
            max-width: 1200px;
            margin: 100px auto 40px;
            padding: 0 20px;
        }

        /* Product Grid */
        .products-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            padding: 1rem;
        }

        .product-card {
            background: white;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 3px 10px rgba(0,0,0,0.1);
            transition: transform 0.3s ease;
        }

        .product-card:hover {
            transform: translateY(-5px);
        }

        .product-image {
            width: 100%;
            height: 250px;
            object-fit: cover;
        }

        .product-info {
            padding: 1.5rem;
        }

        .product-title {
            color: var(--dark-color);
            margin-bottom: 0.5rem;
        }

        .product-price {
            color: var(--primary-color);
            font-weight: bold;
            font-size: 1.2rem;
        }

        .add-to-cart {
            background: var(--primary-color);
            color: white;
            border: none;
            padding: 0.8rem 1.5rem;
            border-radius: 5px;
            cursor: pointer;
            width: 100%;
            margin-top: 1rem;
            transition: background 0.3s ease;
        }

        .add-to-cart:hover {
            background: #ff5252;
        }

        /* Footer */
        footer {
            background: var(--dark-color);
            color: white;
            padding: 2rem;
            margin-top: 4rem;
        }

        .footer-content {
            max-width: 1200px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
        }

        .footer-section h3 {
            margin-bottom: 1rem;
            color: var(--secondary-color);
        }

        @media (max-width: 768px) {
            .products-grid {
                grid-template-columns: 1fr;
            }
            
            .footer-content {
                text-align: center;
            }
        }
    </style>
</head>
<body>
    <header>
        <nav>
            <h1>GBTS BUILDERS SOLUTION LTD</h1>
            <div class="cart-counter">
                <i class="fas fa-shopping-cart"></i>
                <span id="cart-count">0</span>
            </div>
        </nav>
    </header>

    <main>
        <div class="products-grid">
            <!-- Product Cards -->
            <div class="product-card">
                <img src="https://picsum.photos/400/300" alt="Product" class="product-image">
                <div class="product-info">
                    <h3 class="product-title">Featured Products</h3>
                    <p class="product-price">$299.99</p>
                    <button class="add-to-cart" onclick="addToCart()">Add to Cart</button>
                </div>
            </div>

            <!-- Repeat product cards -->
            <!-- Add more products as needed -->
        </div>
    </main>

    <footer>
        <div class="footer-content">
            <div class="footer-section">
                <h3>About Us</h3>
                <p>Our company - GBTS BUILDERS SOLUTION LTD offers a wide range of premium building products, hand & landscaping tools, security cameras, appliances and more... Address: Shop 1, Jubilee market, Opp. St John's church, Ikare Akoko 342211, Ondo state, Nigeria</p>
            </div>
            <div class="footer-section">
                <h3>Customer Service</h3>
                <p>ask@gbtsbuildingmaterials.com</p>
            </div>
            <div class="footer-section">
                <h3>Follow Us</h3>
                <div class="social-icons">
                    <i class="fab fa-facebook"></i>
                    <i class="fab fa-instagram"></i>
                    <i class="fab fa-twitter"></i>
                </div>
            </div>
        </div>
    </footer>

    <script>
        let cartCount = 0;
        
        function addToCart() {
            cartCount++;
            updateCartCount();
            // Add actual cart functionality here
        }

        function updateCartCount() {
            document.getElementById('cart-count').textContent = cartCount;
            localStorage.setItem('cartCount', cartCount);
        }

        // Initialize cart count from localStorage
        window.onload = function() {
            const savedCount = localStorage.getItem('cartCount');
            if (savedCount) {
                cartCount = parseInt(savedCount);
                updateCartCount();
            }
        }
    </script>
</body>
</html>
